{
  "outputDirectory": "portal",
  "functions": {
    "api/chat.js": {
      "memory": 128,
      "maxDuration": 15
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
