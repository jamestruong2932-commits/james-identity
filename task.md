"Claude, hãy điều chỉnh lại Header/Navbar trong file diagnostic.html (hoặc file layout chung) để tối ưu cho Mobile:

Lọc nội dung: Chỉ hiển thị 3 mục: Chẩn đoán, Insight, Academy trên các thiết bị di động (màn hình < 768px).

Ẩn các mục khác: Các mục như 'About', 'QROS' hoặc các icon không cần thiết phải được ẩn đi trên mobile để tránh đè chữ.

Xử lý hiển thị: >    - Sử dụng display: flex và justify-content: space-between cho Header.

Đảm bảo khoảng cách (gap) giữa các chữ đủ rộng để không bị dính vào nhau.

Font size cho mobile nên để khoảng 12px - 14px để giữ tính tinh tế của thương hiệu.

Kỹ thuật: Hãy sử dụng CSS Media Query @media (max-width: 768px) để áp dụng các thay đổi này mà không làm ảnh hưởng đến giao diện Desktop hiện tại."**